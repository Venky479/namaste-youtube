/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.js"], // // we are telling to the tailwindcss to track these files it contains .js, .html, .jsx
  theme: {
    extend: {},
  },
  plugins: [],
}

